wifi manager模块执行流程。

文件位置：[wifi_manager.c](components\system\services\wifi_manager.c)

## WiFi管理器整体架构流程图

```mermaid
graph TD
    A[系统启动] --> B[wifi_manager_init]
    B --> C{WiFi是否已启用?}
    C -->|否| D[跳过初始化]
    C -->|是| E[创建事件组和互斥锁]
    E --> F[初始化网络接口]
    F --> G[创建默认事件循环]
    G --> H[创建WiFi STA接口]
    H --> I[初始化WiFi驱动]
    I --> J[注册事件处理函数]
    J --> K[设置WiFi模式为STA]
    K --> L[启动WiFi]
    L --> M[初始化完成]
```

## WiFi连接流程图

```mermaid
graph TD
    A[wifi_manager_connect_to_ap] --> B{参数验证}
    B -->|失败| C[返回错误]
    B -->|成功| D{WiFi是否启用?}
    D -->|否| E[返回未初始化错误]
    D -->|是| F{已连接到相同SSID?}
    F -->|是| G[返回成功]
    F -->|否| H[断开当前连接]
    H --> I[配置WiFi参数]
    I --> J[开始连接]
    J --> K[等待连接结果]
    K --> L{连接成功?}
    L -->|是| M[获取IP地址]
    L -->|否| N{重试次数<最大值?}
    N -->|是| O[重新连接]
    N -->|否| P[连接失败]
    M --> Q[启动时间同步]
    O --> K
    P --> C
    Q --> R[连接完成]
```

## WiFi事件处理流程图

```mermaid
graph TD
    A[WiFi事件触发] --> B{事件类型判断}
    B -->|STA_START| C[WiFi STA启动]
    B -->|STA_DISCONNECTED| D[处理断开事件]
    B -->|SCAN_DONE| E[扫描完成处理]
    B -->|GOT_IP| F[获得IP地址]
    
    D --> G{重试次数<最大值?}
    G -->|是| H[重新连接]
    G -->|否| I[连接失败]
    H --> J[更新状态为连接中]
    I --> K[更新状态为连接失败]
    
    F --> L[更新状态为已连接]
    L --> M[设置连接成功标志]
    M --> N[启动时间同步]
    
    E --> O[设置扫描完成标志]
    O --> P[更新扫描状态]
```

## SNTP时间同步流程图

```mermaid
graph TD
    A[start_time_sync] --> B[更新状态为时间同步中]
    B --> C[sntp_time_sync_init]
    C --> D{SNTP已初始化?}
    D -->|是| E[跳过初始化]
    D -->|否| F[配置SNTP参数]
    F --> G[设置同步回调]
    G --> H[启动SNTP客户端]
    H --> I[等待时间同步]
    I --> J[sntp_sync_notification_cb]
    J --> K[设置时区为CST-8]
    K --> L[更新状态为时间已同步]
    L --> M{用户SNTP回调存在?}
    M -->|是| N[调用用户回调]
    M -->|否| O[同步完成]
    N --> O
```

## WiFi扫描流程图

```mermaid
graph TD
    A[wifi_manager_start_scan] --> B{WiFi已初始化?}
    B -->|否| C[初始化WiFi]
    B -->|是| D{扫描进行中?}
    D -->|是| E[返回失败]
    D -->|否| F[配置扫描参数]
    F --> G[启动扫描]
    G --> H{阻塞模式?}
    H -->|是| I[等待扫描完成]
    H -->|否| J[立即返回]
    I --> K[wifi_manager_get_scan_results]
    K --> L[获取扫描记录]
    L --> M[转换为用户格式]
    M --> N[返回结果]
```

## WiFi开关控制流程图

```mermaid
graph TD
    A[wifi_manager_set_enabled] --> B{启用WiFi?}
    B -->|是| C[wifi_manager_enable]
    B -->|否| D[wifi_manager_disable]
    
    C --> E{WiFi已启用?}
    E -->|是| F[返回成功]
    E -->|否| G[重新初始化WiFi]
    G --> H[保存状态到NVS]
    
    D --> I{WiFi已禁用?}
    I -->|是| J[返回成功]
    I -->|否| K[断开连接]
    K --> L[停止WiFi]
    L --> M[去初始化WiFi]
    M --> N[清理资源]
    N --> O[保存状态到NVS]
```

## 状态管理流程图

```mermaid
graph TD
    A[update_wifi_status] --> B{状态改变?}
    B -->|否| C[直接返回]
    B -->|是| D[更新内部状态]
    D --> E{新状态为已连接?}
    E -->|是| F[保存SSID]
    E -->|否| G{新状态为断开?}
    G -->|是| H[清空SSID和时间同步标志]
    G -->|否| I[保持当前状态]
    F --> J{状态回调存在?}
    H --> J
    I --> J
    J -->|是| K[调用状态回调]
    J -->|否| L[记录日志]
    K --> L
```

WiFi管理器实现了完整的WiFi连接管理功能，包括连接、断开、扫描、状态管理、时间同步和开关控制等核心功能。整个系统采用事件驱动的架构，通过FreeRTOS的事件组和信号量实现线程安全的操作。